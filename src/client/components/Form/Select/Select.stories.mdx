import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs/blocks';
import Select from './Select';

<Meta title="Design System/Form/Select" component={Select} />

# Select

export const Template = (args) => <Select {...args} />;

<ArgsTable of={Select} />

<Canvas>
	<Story name="Select">
		<Select
			id="select"
			name="select"
			label="Select"
			error="We have an error here"
			options={[
				{ label: 'View', value: 'view' },
				{ label: 'Edit', value: 'edit' },
				{ label: 'Delete', value: 'delete' },
			]}
			selectedValue={(value) => console.log(value)}
		/>
	</Story>
</Canvas>
