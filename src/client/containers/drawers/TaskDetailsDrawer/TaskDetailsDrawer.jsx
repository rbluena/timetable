import { Drawer } from 'antd';

const TaskDetailsDrawer = ({ isOpen }) => (
  <Drawer title="Title" placement="right">
    <h2>Something beatiful</h2>
  </Drawer>
);

export default TaskDetailsDrawer;
